# 필터 & 레이어 구조 제안

> **산업단지 부동산 정보 플랫폼의 필터 및 레이어 시스템 설계**

## 📋 목차

1. [필터 구조](#필터-구조)
2. [레이어 구조](#레이어-구조)
3. [UI/UX 디자인 가이드](#uiux-디자인-가이드)
4. [구현 우선순위](#구현-우선순위)

---

## 필터 구조

### 1️⃣ **지역 필터** (Region Filter)

#### 목적
사용자가 관심 있는 지역의 데이터만 표시

#### 카테고리 구조
```
지역 필터
├── 시/군/구 선택 (다중 선택)
│   ├── 인천 남동구
│   ├── 인천 중구
│   ├── 인천 서구
│   └── ... (전체 11개)
│
└── 읍/면/동 선택 (시군구 선택 시 활성화)
    ├── 남동구 → 구월동, 논현동, 만수동 등
    └── ... (전체 251개 읍면동)
```

#### UI 컴포넌트
- **Multi-Select Dropdown** with Search
- 선택된 지역 Badge 표시
- "전체 선택" / "전체 해제" 버튼

---

### 2️⃣ **거래 유형 필터** (Transaction Type Filter)

#### 목적
실거래가, 매물, 경매 정보를 선택적으로 표시

#### 카테고리 구조
```
거래 유형
├── 🟢 실거래가 (Transaction)
│   └── 최근 거래 내역
│
├── 🔵 매물 (Listing)
│   └── 현재 판매 중인 매물
│
└── 🔴 경매 (Auction)
    └── 경매 진행 중/예정 매물
```

#### UI 컴포넌트
- **Checkbox Group** with Color Badges
- 각 유형별 카운트 표시 (예: "실거래 3,702건")
- 아이콘 + 색상 코드로 시각적 구분

---

### 3️⃣ **가격 필터** (Price Filter)

#### 목적
사용자의 예산 범위에 맞는 매물 검색

#### 카테고리 구조
```
가격 필터
├── 가격 범위 (Range Slider)
│   ├── 최소: 0억 ~ 최대: 1,000억
│   └── 현재 선택: 50억 ~ 300억
│
├── 평당 시세 (Price per Pyeong)
│   ├── 최소: 0만원 ~ 최대: 5,000만원
│   └── 현재 선택: 500만원 ~ 2,000만원
│
└── 빠른 필터 (Quick Filter)
    ├── ~10억 이하
    ├── 10억~50억
    ├── 50억~100억
    └── 100억 이상
```

#### UI 컴포넌트
- **Range Slider** (Dual Thumb)
- 금액 입력 필드 (Direct Input)
- 빠른 필터 Chip Group

---

### 4️⃣ **면적 필터** (Area Filter)

#### 목적
부지 규모에 따른 필터링

#### 카테고리 구조
```
면적 필터
├── 면적 범위 (m²)
│   ├── 최소: 0m² ~ 최대: 50,000m²
│   └── 현재 선택: 500m² ~ 10,000m²
│
├── 평수 변환 표시
│   └── "약 151평 ~ 3,025평"
│
└── 빠른 필터
    ├── ~500m² (소형)
    ├── 500~2,000m² (중형)
    ├── 2,000~10,000m² (대형)
    └── 10,000m² 이상 (초대형)
```

#### UI 컴포넌트
- **Range Slider** with 평수 변환 표시
- 단위 토글 (m² ↔ 평)

---

### 5️⃣ **매물 유형 필터** (Property Type Filter)

#### 목적
시설 종류별 분류

#### 카테고리 구조
```
매물 유형
├── 🏭 공장 (Factory)
│   └── 제조업 시설
│
├── 🏢 지식산업센터 (Knowledge Industry Center)
│   └── 연구개발 + 제조 복합시설
│
├── 📦 창고 (Warehouse)
│   └── 물류 시설
│
└── 🌳 토지 (Land)
    └── 나대지, 개발 가능 토지
```

#### UI 컴포넌트
- **Checkbox Grid** (2x2 레이아웃)
- 아이콘 + 색상 카드 형태

---

### 6️⃣ **산업단지 필터** (Industrial Complex Filter)

#### 목적
특정 산업단지 내 매물 검색

#### 카테고리 구조
```
산업단지 필터
├── 산업단지 선택 (Multi-Select)
│   ├── 남동국가산업단지
│   ├── 인천경제자유구역
│   ├── 주안국가산업단지
│   └── ... (전체 8개)
│
└── 산업단지 유형
    ├── 국가산업단지
    ├── 일반산업단지
    ├── 도시첨단산업단지
    └── 농공단지
```

#### UI 컴포넌트
- **Searchable Multi-Select**
- 지도에서 클릭하여 선택 가능

---

### 7️⃣ **고급 필터** (Advanced Filters)

#### 목적
세밀한 조건 설정

#### 카테고리 구조
```
고급 필터
├── 거래 시기 (Transaction Date)
│   ├── 최근 1개월
│   ├── 최근 3개월
│   ├── 최근 6개월
│   ├── 최근 1년
│   └── 사용자 지정 (Date Range Picker)
│
├── 가격 변동률 (Price Change)
│   ├── 상승 (> 5%)
│   ├── 안정 (-5% ~ +5%)
│   └── 하락 (< -5%)
│
└── 정렬 (Sort)
    ├── 가격 높은 순
    ├── 가격 낮은 순
    ├── 면적 큰 순
    ├── 면적 작은 순
    ├── 최신 등록순
    └── 거리순 (내 위치 기준)
```

#### UI 컴포넌트
- **Accordion** (접을 수 있는 섹션)
- Date Range Picker
- Radio/Checkbox Groups

---

## 레이어 구조

### 1️⃣ **지도 레이어** (Map Layers)

#### 카테고리 구조
```
지도 레이어
├── 🗺️ 배경 지도 (Base Map)
│   ├── 일반 지도 (Default)
│   ├── 위성 지도 (Satellite)
│   ├── 지형도 (Terrain)
│   └── 하이브리드 (Hybrid)
│
├── 📍 행정구역 경계 (Administrative Boundaries)
│   ├── 시/도 경계 (줌 0-8)
│   ├── 시/군/구 경계 (줌 8-12)
│   └── 읍/면/동 경계 (줌 12-14)
│
└── 🎨 데이터 시각화 모드 (Data Visualization)
    ├── 실거래가 히트맵
    ├── 가격 변동률 히트맵
    └── 시각화 OFF
```

#### UI 컴포넌트
- **Segmented Control** (배경 지도 선택)
- **Switch** (경계선 표시 ON/OFF)
- **Radio Group** (시각화 모드)

---

### 2️⃣ **부동산 정보 레이어** (Property Layers)

#### 카테고리 구조
```
부동산 정보 레이어
├── 📐 필지 (Parcels)
│   ├── 줌 14+ 표시
│   ├── 클릭 시 상세 정보
│   └── 색상: 실거래가 기준
│
├── 🏭 산업단지 (Industrial Complexes)
│   ├── 전체 줌 레벨 표시
│   ├── 폴리곤 + 라벨
│   └── 색상: 황색 (#FEF3C7)
│
├── 📦 용지 (Lots)
│   ├── 포커스 모드에서만 표시
│   └── 색상: 파란색 (#EFF6FF)
│
└── 🧪 유치업종 (Industry Types)
    ├── 포커스 모드에서만 표시
    └── 색상: 초록색 (#ECFDF5)
```

#### UI 컴포넌트
- **Checkbox List** with Eye Icon
- 불투명도 슬라이더 (Opacity Slider)

---

### 3️⃣ **시설 마커 레이어** (Facility Markers)

#### 카테고리 구조
```
시설 마커 레이어
├── 🏭 공장 (Factories)
│   ├── 14,193개 마커
│   ├── 줌 레벨별 클러스터링
│   └── 아이콘: 공장 (회색)
│
├── 🏢 지식산업센터 (Knowledge Centers)
│   ├── 81개 마커
│   ├── 클러스터링
│   └── 아이콘: 빌딩 (보라색)
│
├── 🟢 실거래 마커 (Transaction Markers)
│   └── 최근 거래 내역 표시
│
├── 🔵 매물 마커 (Listing Markers)
│   └── 현재 판매 중
│
└── 🔴 경매 마커 (Auction Markers)
    └── 경매 진행 중
```

#### UI 컴포넌트
- **Checkbox List** with Counter
- 예: "공장 (14,193개)"
- 아이콘 + 색상 미리보기

---

### 4️⃣ **분석 레이어** (Analysis Layers)

#### 카테고리 구조
```
분석 레이어
├── 📊 가격 히트맵 (Price Heatmap)
│   ├── 저가 (파랑) → 고가 (빨강)
│   └── 불투명도 조절 가능
│
├── 📈 가격 변동률 히트맵 (Price Change Heatmap)
│   ├── 하락 (파랑) → 중립 (회색) → 상승 (빨강)
│   └── 최근 1년 기준
│
├── 🔥 거래량 히트맵 (Transaction Volume Heatmap)
│   ├── 거래 빈도 표시
│   └── 인기 지역 파악
│
└── 🎯 관심 영역 (Area of Interest)
    ├── 사용자가 지정한 영역 강조
    └── 다각형/원형 그리기 도구
```

#### UI 컴포넌트
- **Radio Group** (한 번에 하나만 선택)
- Opacity Slider
- Drawing Tools

---

### 5️⃣ **3D 뷰 레이어** (3D View Layers)

#### 카테고리 구조
```
3D 뷰
├── 🏙️ 건물 높이 (Building Height)
│   ├── 지식산업센터 층수 기반
│   └── 3D 렌더링
│
├── 📏 지형 고도 (Terrain Elevation)
│   └── 실제 지형 높이
│
└── 🎬 카메라 컨트롤
    ├── Pitch (기울기): 0° ~ 60°
    ├── Bearing (회전): 0° ~ 360°
    └── Reset 버튼
```

#### UI 컴포넌트
- **Toggle Switch** (3D 모드 ON/OFF)
- Slider (Pitch/Bearing 조절)

---

## UI/UX 디자인 가이드

### 1. **일관된 디자인 시스템**

#### 색상 팔레트
```typescript
const COLORS = {
  // Primary
  primary: '#228BE6',      // 파랑
  success: '#12B886',      // 초록
  warning: '#FAB005',      // 노랑
  danger: '#FA5252',       // 빨강

  // Functional
  transaction: '#12B886',  // 실거래 (초록/티엘)
  listing: '#228BE6',      // 매물 (파랑)
  auction: '#FA5252',      // 경매 (빨강)

  // Facilities
  factory: '#64748B',      // 공장 (슬레이트 그레이)
  knowledgeCenter: '#9775FA', // 지식산업센터 (보라)
  complex: '#FAB005',      // 산업단지 (황색)

  // Neutral
  gray: ['#F8F9FA', '#E9ECEF', '#DEE2E6', '#CED4DA', '#ADB5BD', '#868E96', '#495057', '#343A40', '#212529']
};
```

#### 타이포그래피
```typescript
const TYPOGRAPHY = {
  // Headings
  h1: { size: 32, weight: 700 },
  h2: { size: 24, weight: 700 },
  h3: { size: 20, weight: 600 },
  h4: { size: 16, weight: 600 },

  // Body
  body: { size: 14, weight: 500 },
  small: { size: 12, weight: 500 },
  tiny: { size: 10, weight: 500 },
};
```

#### 간격 (Spacing)
```typescript
const SPACING = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
};
```

#### 라운드 처리 (Border Radius)
```typescript
const RADIUS = {
  xs: 4,
  sm: 6,
  md: 8,
  lg: 12,
  xl: 16,
  full: 9999,
};
```

#### 그림자 (Shadow)
```typescript
const SHADOWS = {
  xs: '0 1px 3px rgba(0, 0, 0, 0.05)',
  sm: '0 2px 8px rgba(0, 0, 0, 0.04)',
  md: '0 4px 12px rgba(0, 0, 0, 0.08)',
  lg: '0 8px 24px rgba(0, 0, 0, 0.12)',
  xl: '0 12px 48px rgba(0, 0, 0, 0.16)',
};
```

---

### 2. **레이아웃 구조**

#### 필터 패널 (좌측)
```
┌─────────────────────────────┐
│ 📁 필터                      │
│ ─────────────────────────── │
│                             │
│ ▼ 지역 필터                 │
│   □ 남동구                  │
│   □ 중구                    │
│   ...                       │
│                             │
│ ▼ 거래 유형                 │
│   ☑ 실거래가 (3,702건)       │
│   ☑ 매물 (1,754건)          │
│   ☑ 경매 (333건)            │
│                             │
│ ▼ 가격 필터                 │
│   [====○======○====]        │
│   50억 ~ 300억              │
│                             │
│ ...                         │
│                             │
│ [초기화] [적용]              │
└─────────────────────────────┘
```

#### 레이어 패널 (우측)
```
┌─────────────────────────────┐
│ 🗺️ 레이어                   │
│ ─────────────────────────── │
│                             │
│ 배경 지도                    │
│  ●일반 ○위성 ○지형 ○하이브리드│
│                             │
│ ▼ 부동산 정보                │
│   ☑ 👁️ 필지 (43,266개)      │
│   ☑ 👁️ 산업단지 (8개)       │
│   □ 👁️ 용지                 │
│   □ 👁️ 유치업종             │
│                             │
│ ▼ 시설 마커                  │
│   ☑ 👁️ 공장 (14,193개)      │
│   ☑ 👁️ 지식산업센터 (81개)   │
│                             │
│ ▼ 데이터 시각화              │
│   ○ OFF                     │
│   ○ 실거래가 히트맵          │
│   ○ 가격 변동률 히트맵        │
│                             │
│ 3D 뷰 [ OFF ───○ ON ]       │
└─────────────────────────────┘
```

---

### 3. **상호작용 패턴**

#### 필터 적용 방식
1. **즉시 적용 (Instant)**: Checkbox, Switch 변경 시 즉시 반영
2. **지연 적용 (Debounced)**: Range Slider, Input 변경 시 0.5초 후 반영
3. **명시적 적용 (Explicit)**: "적용" 버튼 클릭 시 반영

#### 레이어 토글
- **클릭**: 레이어 ON/OFF
- **눈 아이콘**: 시각적 피드백
- **슬라이더**: 불투명도 조절

#### 반응형 디자인
- **Desktop (>1280px)**: 필터/레이어 패널 고정
- **Tablet (768-1280px)**: 패널 Drawer 형태
- **Mobile (<768px)**: 하단 Bottom Sheet

---

## 구현 우선순위

### Phase 1 (필수 - 즉시 구현)
1. ✅ 지역 필터 (시군구/읍면동)
2. ✅ 거래 유형 필터 (실거래/매물/경매)
3. ✅ 필지 레이어
4. ✅ 산업단지 레이어
5. ✅ 공장/지식산업센터 마커 레이어

### Phase 2 (중요 - 다음 스프린트)
1. 가격 필터 (Range Slider)
2. 면적 필터
3. 매물 유형 필터
4. 데이터 시각화 (히트맵)
5. 레이어 불투명도 조절

### Phase 3 (추가 기능 - 향후)
1. 고급 필터 (거래 시기, 정렬)
2. 산업단지 필터
3. 3D 뷰 레이어
4. 관심 영역 그리기
5. 커스텀 필터 저장 기능

---

## 기술 스택 제안

### 상태 관리
- **Zustand**: 필터/레이어 상태 전역 관리
- Store 구조:
```typescript
interface FilterLayerStore {
  // Filters
  filters: {
    regions: string[];
    transactionTypes: ('transaction' | 'listing' | 'auction')[];
    priceRange: [number, number];
    areaRange: [number, number];
    propertyTypes: ('factory' | 'knowledge-center' | 'warehouse' | 'land')[];
  };

  // Layers
  layers: {
    basemap: 'default' | 'satellite' | 'terrain' | 'hybrid';
    parcels: { visible: boolean; opacity: number };
    complexes: { visible: boolean; opacity: number };
    factories: { visible: boolean; opacity: number };
    knowledgeCenters: { visible: boolean; opacity: number };
    visualization: 'off' | 'price' | 'price-change';
  };

  // Actions
  setFilter: (key: string, value: any) => void;
  toggleLayer: (key: string) => void;
  setLayerOpacity: (key: string, opacity: number) => void;
  resetFilters: () => void;
}
```

### UI 컴포넌트
- **Mantine UI**: 이미 사용 중인 컴포넌트 라이브러리
  - `Select`, `MultiSelect`: 드롭다운
  - `RangeSlider`: 가격/면적 범위
  - `Checkbox`, `Switch`: 필터/레이어 토글
  - `Drawer`, `Accordion`: 패널 레이아웃

---

## 참고 자료

- **디자인 영감**:
  - [Zillow](https://www.zillow.com/) - 부동산 필터 시스템
  - [Google Maps](https://maps.google.com/) - 레이어 시스템
  - [Mapbox Studio](https://studio.mapbox.com/) - 데이터 시각화

- **UX 패턴**:
  - [Material Design Filtering](https://material.io/components/chips)
  - [Apple Maps Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/maps)

---

**문서 버전**: 1.0
**최종 수정**: 2025-01-01
**작성자**: Claude Code
**검토 필요**: UI/UX 디자이너, 프론트엔드 개발자
